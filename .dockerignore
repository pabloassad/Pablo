# The converter image only needs server/. Everything else — the Next.js site
# and ~100 MB of photos and video — would only bloat the build context.
node_modules
.next
.git
app
components
lib
public
*.jpg
*.png
*.svg
*.mp4
*.wav
package-lock.json
package.json

# ...but the service itself must always be included, UI assets and all.
!server
